

const Departement = function(departement) {
  this.id_departements = departement.id_departements
  this.nom = departement.nom;
  this.created_at = departement.created_at
};

module.exports = Departement;
